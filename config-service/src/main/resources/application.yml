spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: file:///D:/Documentos/A POLI/Tecnologia/Cuarto Semestre/PDS/configdata
  security:
    user:
      name: ${CONFIG_USER}
      password: ${CONFIG_PASSWORD}
server:
  port: 9000
